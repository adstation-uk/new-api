# 模型文档测试

这是为一个模型文档准备的测试 MDX 文件。

## 支持的特性

- **Gfm**: GitHub Flavored Markdown
- **Components**: 可以直接在 MDX 中使用 React 组件
- **Dynamic**: 内容可以从 `website/doc` 文件夹动态加载

### 样例代码

```python
print("Hello, New API!")
```

> 这是一个引用块。
